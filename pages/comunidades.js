import Layout from "../components/layout";

const comunidades = () => {
    return (
        <Layout>
        <main>

            <h1>Comunidades</h1>
        </main>
        </Layout>
    );
}

export default comunidades;